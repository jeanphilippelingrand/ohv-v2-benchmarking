-- DDL generated by Postico 1.4.2
-- Not all database features are supported. Do not use for backup.

-- Table Definition ----------------------------------------------

CREATE TABLE related_products (
    product_id1 integer NOT NULL REFERENCES products(id),
    product_id2 integer NOT NULL REFERENCES products(id),
    score numeric(10,9) NOT NULL
);

-- Indices -------------------------------------------------------

CREATE INDEX product_id1_idx ON related_products(product_id1);
